services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:80"
    networks:
      - wowspelldle-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  wowspelldle-network:
    driver: bridge