services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:80"
    networks:
      - wowspelldle-network

networks:
  wowspelldle-network:
    driver: bridge